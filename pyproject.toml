[project]
name = "trialdiff"
version = "0.1.0"
description = "ClinicalTrials.gov study-record history harvester and materiality classifier scaffold."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
trialdiff = "trialdiff.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
