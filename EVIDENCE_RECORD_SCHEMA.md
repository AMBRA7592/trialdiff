# Evidence Record Schema

> **Scope note (2026-07-14):** this document describes only the frozen
> v0.1-alpha format (`trialdiff.alpha_demo_record`). The current format
> (`trialdiff.evidence_record`, used by the event-class packages) is
> documented in `DATA_DICTIONARY.md`, with machine-readable JSON Schemas for
> both formats in `schemas/`.

The frozen files in `records/` use:

```text
schema: trialdiff.alpha_demo_record
demo_version: v0.1-alpha
```

Each record is a self-contained export wrapper around the canonical TrialDiff Evidence Record generated by the Python pipeline. Frozen v0.1-alpha records are byte-preserved; do not edit their payloads to reinterpret severity.

## Top-Level Fields

- `schema` - exported record schema identifier.
- `demo_version` - frozen demo version.
- `event_id` - deterministic Evidence Record ID.
- `study` - trial metadata needed for inspection.
- `versions` - from/to ClinicalTrials.gov version references and submitted date.
- `classification` - deterministic classification output.
- `changed_paths` - JSON Pointer paths changed by the patch.
- `patch` - JSON Patch operations.
- `claims_supported` - claims supported by the source data and deterministic rules.
- `claims_not_supported` - explicit non-claims.
- `review_question` - neutral question a reviewer should ask.
- `provenance` - hashes and source fields.
- `live_urls` - deployed Evidence Record, canonical JSON, and trial pages.
- `citation_text` - short citation string.
- `source_corpus` - alpha corpus and selection metadata.
- `canonical_evidence_record` - the canonical generated Evidence Record payload.

## Required Study Fields

- `nct_id`
- `title`
- `official_title`
- `lead_sponsor`
- `lead_sponsor_class`
- `clinicaltrials_gov_url`
- `official_v2_api_url`
- `current_record_hash`
- `current_record_raw_hash`

## Required Classification Fields

The `severity` fields are deterministic, reproducible, uncalibrated triage metadata. They are not validated review-priority labels. Existing frozen records keep these fields byte-identical for manifest provenance. Future regenerated records may add an explicit `calibration_status: uncalibrated` field instead of renaming historical severity values.

- `severity_pre_timing`
- `severity`
- `category`
- `categories`
- `timing_context`
- `deterministic_rules`
- `value_signals`
- `rule_set_hash`

## Required Provenance Fields

- `evidence_version`
- `evidence_canonical_hash`
- `evidence_source`
- `evidence_source_url`
- `patch_hash`
- `patch_source`
- `patch_source_url`
- `patch_raw_hash`
- `from_snapshot_hash`
- `to_snapshot_hash`
- `materiality_event_hash`

## Stable ID Rule

Evidence IDs are deterministic over:

- NCT ID;
- from/to version;
- patch hash;
- headline category;
- sorted changed paths;
- rule-set hash;
- evidence version.

Changing the source patch, rule set, headline category, or evidence template version changes the generated ID.
